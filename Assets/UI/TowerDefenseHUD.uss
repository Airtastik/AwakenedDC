/* ═══════════════════════════════════════════════════
   TowerDefenseHUD.uss
   Matches RPGins dark theme: near-black bg, gold accents
   ═══════════════════════════════════════════════════ */

/* ── Root ──────────────────────────────────────────── */
.hud-root {
    position: absolute;
    left: 0; right: 0; top: 0; bottom: 0;
    flex-direction: column;
    justify-content: space-between;
}

.hidden { display: none; }

/* ── Top bar ───────────────────────────────────────── */
.top-bar {
    position: absolute;
    left: 0; right: 0; top: 0;
    flex-direction: row;
    justify-content: center;
    padding: 6px 12px;
    background-color: rgba(13, 17, 23, 0.92);
    border-bottom-width: 1px;
    border-color: rgb(48, 54, 61);
}

/* ── Stat pills ────────────────────────────────────── */
.stat-pill {
    flex-direction: row;
    align-items: center;
    background-color: rgb(22, 27, 34);
    border-width: 1px;
    border-color: rgb(48, 54, 61);
    border-radius: 5px;
    padding: 4px 12px;
    margin: 0 6px;
    min-width: 90px;
}

.health-pill   { border-color: rgb(248, 81, 73); }
.wave-pill     { border-color: rgb(240, 192, 64); }
.enemy-pill    { border-color: rgb(224, 92, 92); }
.currency-pill { border-color: rgb(78, 205, 196); }

.stat-icon {
    font-size: 14px;
    margin-right: 6px;
    -unity-font-style: bold;
}
.health-icon   { color: rgb(248, 81, 73); }
.wave-icon     { color: rgb(240, 192, 64); }
.enemy-icon    { color: rgb(224, 92, 92); }
.currency-icon { color: rgb(78, 205, 196); }

.stat-value {
    font-size: 14px;
    -unity-font-style: bold;
    color: rgb(230, 237, 243);
    margin-right: 4px;
}

.stat-label {
    font-size: 8px;
    color: rgb(139, 148, 158);
    letter-spacing: 1px;
    -unity-font-style: bold;
    align-self: flex-end;
    margin-bottom: 1px;
}

/* ── Tower shop ────────────────────────────────────── */
.tower-shop {
    position: absolute;
    left: 0; right: 0; bottom: 0;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    padding: 6px 12px;
    background-color: rgba(13, 17, 23, 0.92);
    border-top-width: 1px;
    border-color: rgb(48, 54, 61);
}

.shop-label {
    font-size: 9px;
    color: rgb(139, 148, 158);
    letter-spacing: 3px;
    -unity-font-style: bold;
    margin-right: 12px;
    align-self: center;
}

/* ── Tower buttons ─────────────────────────────────── */
.tower-btn {
    flex-direction: column;
    align-items: center;
    background-color: rgb(22, 27, 34);
    border-width: 1px;
    border-color: rgb(48, 54, 61);
    border-radius: 5px;
    padding: 6px 8px;
    margin: 0 5px;
    min-width: 70px;
    transition-duration: 0.1s;
    transition-property: border-color, background-color;
}

.tower-btn:hover {
    border-color: rgb(240, 192, 64);
    background-color: rgb(30, 26, 14);
}

.tower-btn.selected {
    border-color: rgb(78, 205, 196);
    background-color: rgb(14, 30, 30);
}

.tower-btn.unaffordable {
    opacity: 0.4;
}

/* ── Tower placeholder icons ───────────────────────── */
.tower-icon {
    width: 36px;
    height: 36px;
    border-radius: 4px;
    border-width: 1px;
    margin-bottom: 4px;
}

/* Each tower gets a distinct colour block as placeholder */
.t1-icon {
    background-color: rgb(56, 100, 200);
    border-color: rgb(88, 140, 255);
}
.t2-icon {
    background-color: rgb(180, 60, 60);
    border-color: rgb(224, 92, 92);
}
.t3-icon {
    background-color: rgb(60, 140, 180);
    border-color: rgb(78, 205, 196);
}
.t4-icon {
    background-color: rgb(120, 60, 180);
    border-color: rgb(170, 88, 255);
}

.tower-name {
    font-size: 9px;
    color: rgb(201, 209, 217);
    -unity-font-style: bold;
    letter-spacing: 1px;
    -unity-text-align: upper-center;
}

.tower-cost {
    font-size: 9px;
    color: rgb(78, 205, 196);
    -unity-text-align: upper-center;
    margin-top: 2px;
}

.tower-hotkey {
    font-size: 8px;
    color: rgb(110, 118, 129);
    -unity-text-align: upper-center;
}

/* ── Wave message ──────────────────────────────────── */
.wave-message {
    position: absolute;
    left: 25%; right: 25%;
    top: 10%;
    font-size: 20px;
    -unity-font-style: bold;
    color: rgb(240, 192, 64);
    letter-spacing: 4px;
    -unity-text-align: upper-center;
    background-color: rgba(22, 27, 34, 0.92);
    border-width: 1px;
    border-color: rgb(240, 192, 64);
    border-radius: 6px;
    padding: 10px 20px;
}

/* ── Bottom bar wrapper ─────────────────────────────── */
.bottom-bar {
    position: absolute;
    left: 0; right: 0; bottom: 0;
    flex-direction: row;
    align-items: stretch;
    background-color: rgba(13, 17, 23, 0.92);
    border-top-width: 1px;
    border-color: rgb(48, 54, 61);
}

/* ── Tower shop (now inside bottom-bar) ─────────────── */
/* Override position — now flows in the row */
.tower-shop {
    position: relative;
    left: auto; right: auto; bottom: auto;
    border-top-width: 0;
    border-right-width: 1px;
    border-color: rgb(48, 54, 61);
    flex-shrink: 0;
}

/* ── Upgrade panel ──────────────────────────────────── */
.upgrade-panel {
    flex-direction: column;
    justify-content: center;
    padding: 8px 16px;
    flex-grow: 1;
    min-width: 260px;
}

.upgrade-panel.hidden { display: none; }

.upgrade-header {
    flex-direction: row;
    align-items: center;
    margin-bottom: 6px;
}

.upgrade-title {
    font-size: 13px;
    -unity-font-style: bold;
    color: rgb(230, 237, 243);
    letter-spacing: 2px;
    flex-grow: 1;
}

.upgrade-level-badge {
    font-size: 10px;
    -unity-font-style: bold;
    color: rgb(13, 17, 23);
    background-color: rgb(240, 192, 64);
    border-radius: 3px;
    padding: 2px 7px;
    letter-spacing: 1px;
}

.upgrade-stats {
    flex-direction: row;
    margin-bottom: 8px;
}

.upgrade-stat {
    font-size: 10px;
    color: rgb(139, 148, 158);
    margin-right: 14px;
    letter-spacing: 1px;
}

.upgrade-actions {
    flex-direction: row;
    align-items: center;
}

.upgrade-btn {
    height: 28px;
    padding: 0 12px;
    margin-right: 6px;
    border-radius: 4px;
    border-width: 1px;
    border-color: rgb(240, 192, 64);
    background-color: rgb(38, 32, 10);
    color: rgb(240, 192, 64);
    font-size: 10px;
    -unity-font-style: bold;
}

.upgrade-btn:hover { background-color: rgb(60, 50, 14); }
.upgrade-btn:disabled { opacity: 0.4; }

.sell-btn {
    height: 28px;
    padding: 0 12px;
    margin-right: 6px;
    border-radius: 4px;
    border-width: 1px;
    border-color: rgb(224, 92, 92);
    background-color: rgb(38, 10, 10);
    color: rgb(224, 92, 92);
    font-size: 10px;
    -unity-font-style: bold;
}

.sell-btn:hover { background-color: rgb(60, 16, 16); }

.deselect-btn {
    height: 28px;
    width: 28px;
    border-radius: 4px;
    border-width: 1px;
    border-color: rgb(48, 54, 61);
    background-color: rgb(22, 27, 34);
    color: rgb(139, 148, 158);
    font-size: 12px;
}

.deselect-btn:hover { border-color: rgb(139, 148, 158); color: rgb(201, 209, 217); }

/* Max level — hide upgrade button */
.upgrade-btn.max-level {
    border-color: rgb(78, 205, 196);
    background-color: rgb(10, 26, 26);
    color: rgb(78, 205, 196);
    opacity: 1;
}
